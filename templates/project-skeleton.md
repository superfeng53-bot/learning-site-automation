# Project Skeleton (annotated)

Generated by `scripts/init_project.py`. After phase 1 finishes, only the `<pkg>/` half is populated. After phase 5 the `<svc>/` half fills in. Items marked `[stub]` are zero-byte placeholders that subsequent phases must replace.

```
<project_root>/
├── README.md                     [phase 6] one-page operator doc
├── LICENSE                       MIT or your choice
├── .gitignore                    excludes data/, .run/, *.venv*, __pycache__
├── pyproject.toml                [phase 6] metadata + deps
├── requirements.txt              minimal runtime deps
├── requirements-build.txt        pyinstaller (phase 6)
├── run_service.py                [phase 5] FastAPI entry with single-instance lock
├── run_course.py                 [phase 4] single-account CLI runner
├── <pkg>_login.py                [phase 1] shortcut: python <pkg>_login.py
├── start.sh / start.bat          [phase 6]
├── build.sh / build.bat          [phase 6]
│
├── <pkg>/                        HTTP toolkit
│   ├── __init__.py               re-exports get_session_manager
│   ├── __main__.py               python -m <pkg> -> help
│   ├── config.py                 BASE_URL / paths / captcha std dims
│   ├── client.py                 HttpClient with form_post / form_post_safe
│   ├── captcha.py                [phase 1] solver(s) for the site's captcha kind
│   ├── captcha_limiter.py        [phase 3] cooldown + per-username locks
│   ├── login.py                  [phase 1] LoginService + LoginResult
│   ├── session_manager.py        [phase 3] per-user_id clients + ensure_session
│   ├── responses.py              [phase 2] ApiResponse + hint dicts
│   ├── <domain1>.py              e.g. course.py
│   ├── <domain2>.py              e.g. study.py
│   ├── <domain3>.py              e.g. exam.py
│   ├── <domain4>.py              e.g. credit.py
│   ├── <domain5>.py              e.g. member.py
│   ├── <domain6>.py              [optional] e.g. recharge.py
│   ├── course_runner.py          [phase 4] end-to-end orchestrator
│   ├── account_pipeline.py       [phase 5] subject mapping + planning glue
│   ├── subject_mapper.py         [phase 5, optional] AI classification
│   ├── course_planner.py         [phase 5] DP knapsack to hit credit target
│   ├── cli_login.py              CLI entries
│   ├── cli_<domain>.py           one per service
│   ├── README.md                 short toolkit doc
│   └── API_REFERENCE.md          [phase 2] endpoint-by-endpoint
│
├── <svc>/                        always-on service (phase 5+)
│   ├── __init__.py               __version__
│   ├── __main__.py               python -m <svc>
│   ├── config.py                 service constants: TICK_SECONDS, CONCURRENCY_*, DAILY_START_HOUR
│   ├── runtime.py                SingleInstanceLock + find_available_port + resource_path
│   ├── orchestrator.py           tick loop
│   ├── worker.py                 AccountWorker.run_once
│   ├── apply_worker.py           ApplyWorker.process_one
│   ├── persistence/
│   │   ├── __init__.py
│   │   ├── schema.py             SCHEMA_SQL string
│   │   └── store.py              AccountStore (sqlite WAL)
│   └── web/
│       ├── __init__.py
│       ├── app.py                FastAPI
│       ├── excel_io.py           [phase 5] 中文模板/导入/导出 (excel-spec.md)
│       └── templates/
│           └── index.html        [phase 5] 简体中文控制台 (web-ui-spec.md)
│
├── docs/
│   ├── LOGIN_FLOW.md             [phase 1] frontend + captcha protocol
│   ├── handoffs/                 Cursor context handoffs (PHASE<N>_*.md)
│   ├── api-discovery/            [phase 2, optional] per-domain recon drafts
│   └── 通用需求说明.md            [phase 5] adapted from templates/requirements.md
│
├── data/                         runtime, gitignored except .gitkeep
│   ├── .gitkeep
│   ├── account.json              [phase 1] {platform, username, password}
│   ├── cookies.json              [phase 1+] saved by cli_login
│   ├── captcha-state.json        [phase 3] global cooldown state
│   └── course-state-*.json       [phase 4+] resume hints
│
├── .run/                         service runtime, gitignored
│   ├── service/
│   │   └── service.lock          single-instance lock
│   ├── sww_service.db            SQLite (named per project)
│   └── ai_config.json            LLM credentials (optional)
│
├── scripts/
│   ├── build.py                  [phase 6] PyInstaller driver
│   ├── start.py                  [phase 6, optional] start.sh's Python alternative
│   └── <site>.spec.template      [phase 6] PyInstaller spec
│
└── .github/
    └── workflows/
        └── ci.yml                [phase 6] lint + import smoke
```

## Naming Conventions

| Slot | Example (shuangwei) | Rule |
|------|---------------------|------|
| `<pkg>` | `sww_api` | snake_case, suffix `_api`, derived from domain |
| `<svc>` | `sww_service` | same prefix as `<pkg>`, suffix `_service` |
| `<DOMAIN>` | `course` | the central business object (lesson, course, project, exercise) |
| binary name | `双卫网_27_05` | `<site-CJK-name>_<DD>_<MM>` |
| db file | `sww_service.db` | `<svc>.db` |
| Excel template | `医博士账号模板.xlsx` | `<UI-platform-name>账号模板.xlsx` |
| cookies file | `cookies.json` | always plural even for one account |

Keep `<pkg>` short — it's typed in every `python -m <pkg>.cli_*` invocation.
